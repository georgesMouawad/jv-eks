apiVersion: v1
kind: ServiceAccount
metadata:
    name: crate-service
    annotations:
        # Filled by the overlay patch with the IRSA role ARN from tofu output crate_service_irsa_role_arn
        eks.amazonaws.com/role-arn: ''
