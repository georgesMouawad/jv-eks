# Seal using the prod public cert (see k8s/certs/sealed-secrets-prod.pem).
#
#   kubectl create secret generic jwt-secret \
#     --namespace jv-eks \
#     --from-literal=JWT_SECRET=<prod-jwt-key> \
#     --dry-run=client -o yaml | \
#   kubeseal --cert k8s/certs/sealed-secrets-prod.pem --format yaml \
#     > k8s/overlays/prod/sealed-secrets/jwt-secret.yaml
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
    name: jwt-secret
    namespace: jv-eks
spec:
    encryptedData:
        JWT_SECRET: REPLACE_WITH_KUBESEAL_OUTPUT
    template:
        metadata:
            name: jwt-secret
            namespace: jv-eks
        type: Opaque
